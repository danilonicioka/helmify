# Standard Generic Pipeline Template
# This is a simple generic placeholder. Organizations should mount their own pipeline or override this.

stages:
  - build
  - deploy

variables:
  APP_NAME: $CI_PROJECT_NAME

build_image:
  stage: build
  image: registry.example.com/devops/runner-tools:latest
  script:
    - echo "Building generic container image for $APP_NAME..."
    - # Build commands go here

deploy_helm:
  stage: deploy
  image: registry.example.com/devops/runner-tools:latest
  script:
    - echo "Deploying generic Helm chart for $APP_NAME..."
    - # Deploy commands go here