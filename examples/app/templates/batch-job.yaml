apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "app.fullname" . }}-batch-job
  labels:
  {{- include "app.labels" . | nindent 4 }}
spec:
  backoffLimit: {{ .Values.batch.backoffLimit }}
  template:
    spec:
      containers:
      - command:
        - perl
        - -Mbignum=bpi
        - -wle
        - print bpi(2000)
        env:
        - name: KUBERNETES_CLUSTER_DOMAIN
          value: {{ quote .Values.kubernetesClusterDomain }}
        image: {{ .Values.batch.pi.image.repository }}:{{ .Values.batch.pi.image.tag
          | default .Chart.AppVersion }}
        name: pi
        resources: {}
      nodeSelector: {{- toYaml .Values.batch.nodeSelector | nindent 8 }}
      restartPolicy: Never
      serviceAccountName: {{ include "app.serviceAccountName" . }}
      tolerations: {{- toYaml .Values.batch.tolerations | nindent 8 }}
      topologySpreadConstraints: {{- toYaml .Values.batch.topologySpreadConstraints
        | nindent 8 }}
