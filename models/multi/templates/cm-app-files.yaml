{{- $comp := index .Values "app" | default dict -}}
{{- if ne $comp.enabled false }}
{{- if and $comp.files $comp.files.cm -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "chart-model-multi.fullname" . }}-app-files
  labels:
    {{- include "chart-model-multi.labels" . | nindent 4 }}
data:
{{- range $filename, $config := $comp.files.cm }}
  {{ $filename }}: |
    {{- $config.content | nindent 4 }}
{{- end }}
{{- end }}

{{- end }}
