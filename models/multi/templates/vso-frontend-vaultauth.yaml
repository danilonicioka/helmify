{{- $comp := index .Values "frontend" | default dict -}}
{{- if and $comp.vso $comp.vso.enabled }}
apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: {{ include "chart-model-multi.fullname" . }}-frontend-vault-auth
  labels:
    {{- include "chart-model-multi.labels" . | nindent 4 }}
spec:
  method: kubernetes
  mount: kubernetes
  kubernetes:
    role: {{ $comp.vso.role | quote }}
    serviceAccount: {{ include "chart-model-multi.fullname" . }}-frontend-sa
{{- end }}
