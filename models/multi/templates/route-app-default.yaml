{{- $comp := index .Values "app" | default dict -}}
{{- if ne $comp.enabled false }}
{{- if and $comp $comp.route $comp.route.default.enabled -}}
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ include "chart-model-multi.fullname" . }}-app
  labels:
    {{- include "chart-model-multi.component.labels" (dict "context" . "component" "app") | nindent 4 }}
  {{- with $comp.route.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if $comp.route.default.host }}
  host: {{ $comp.route.default.host | quote }}
  {{- end }}
  {{- if $comp.route.path }}
  path: {{ $comp.route.path | quote }}
  {{- end }}
  {{- if $comp.route.tls }}
  tls:
    {{- toYaml $comp.route.tls | nindent 4 }}
  {{- end }}
  to:
    kind: Service
    name: {{ include "chart-model-multi.fullname" . }}-app
    weight: 100
  port:
    targetPort: http
  wildcardPolicy: None
{{- end }}

{{- end }}
