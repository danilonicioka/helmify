
{{- $comp := index .Values "api" | default dict -}}
{{- if ne $comp.enabled false }}
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ include "chart-model-multi.fullname" . }}-api
  labels:
    {{- include "chart-model-multi.component.labels" (dict "context" . "component" "api") | nindent 4 }}
  {{- $annotations := include "chart-model-multi.component.annotations" (dict "context" . "component" "api") }}
  {{- if $annotations }}
  annotations:
    {{- $annotations | nindent 4 }}
  {{- end }}
spec:
  schedule: {{ $comp.schedule | quote }}
  concurrencyPolicy: {{ default "Allow" $comp.concurrencyPolicy }}
  successfulJobsHistoryLimit: {{ default 3 $comp.successfulJobsHistoryLimit }}
  failedJobsHistoryLimit: {{ default 1 $comp.failedJobsHistoryLimit }}
  jobTemplate:
    metadata:
      labels:
        {{- include "chart-model-multi.component.selectorLabels" (dict "context" . "component" "api") | nindent 8 }}
    spec:
      template:
        metadata:
          annotations:
            {{- if and $comp.files $comp.files.cm (not (empty $comp.files.cm)) }}
            checksum/cm-files: '{{ include (print $.Template.BasePath "/cm-api-files.yaml") . | sha256sum }}'
            {{- end }}
            {{- if and $comp.files $comp.files.secret (not (empty $comp.files.secret)) }}
            checksum/secret-files: '{{ include (print $.Template.BasePath "/secret-api-files.yaml") . | sha256sum }}'
            {{- end }}
            {{- if and $comp.secret (not (empty $comp.secret)) }}
            checksum/secret: '{{ include (print $.Template.BasePath "/secret-api.yaml") . | sha256sum }}'
            {{- end }}
          labels:
            {{- include "chart-model-multi.component.selectorLabels" (dict "context" . "component" "api") | nindent 12 }}
        spec:
          {{- with $comp.image.pullSecrets }}
          imagePullSecrets:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- if and $comp.vso $comp.vso.enabled }}
          serviceAccountName: {{ include "chart-model-multi.fullname" . }}-api-sa
          {{- else if .Values.serviceAccount }}
          serviceAccountName: {{ include "chart-model-multi.serviceAccountName" . }}
          {{- end }}
          containers:
            - name: api
              image: "{{ $comp.image.repository }}:{{ $comp.image.tag | default .Chart.AppVersion }}"
              imagePullPolicy: {{ $comp.image.pullPolicy }}
              {{- if $comp.command }}
              command:
                {{- toYaml $comp.command | nindent 16 }}
              {{- end }}
              {{- if $comp.args }}
              args:
                {{- toYaml $comp.args | nindent 16 }}
              {{- end }}
              envFrom:
                - configMapRef:
                    name: {{ include "chart-model-multi.fullname" . }}-global-cm
                - secretRef:
                    name: {{ include "chart-model-multi.fullname" . }}-global-secret
                {{- if and $comp.cm (not (empty $comp.cm)) }}
                - configMapRef:
                    name: {{ include "chart-model-multi.fullname" . }}-api-cm
                {{- end }}
                {{- if and $comp.secret (not (empty $comp.secret)) }}
                - secretRef:
                    name: {{ include "chart-model-multi.fullname" . }}-api-secret
                {{- end }}
                {{- if and $comp.vso $comp.vso.enabled }}
                - secretRef:
                    name: {{ include "chart-model-multi.fullname" . }}-api-vso-secret
                {{- end }}
              {{- if $comp.resources }}
              resources:
                {{- toYaml $comp.resources | nindent 16 }}
              {{- end }}
              volumeMounts:
                                {{- if and $comp.truststore $comp.truststore.enabled }}
                - name: truststore
                  mountPath: /etc/pki/ca-trust/extracted/pem/tls-ca-bundle.pem
                  subPath: tls-ca-bundle.pem
                  readOnly: true
                {{- end }}
                {{- if and $comp.persistence $comp.persistence.enabled }}
                - name: api-data
                  mountPath: {{ $comp.persistence.mountPath | default "/data" }}
                  {{- if $comp.persistence.subPath }}
                  subPath: {{ $comp.persistence.subPath }}
                  {{- end }}
                {{- end }}
                {{- if and $comp.files $comp.files.cm }}
                {{- range $key, $val := $comp.files.cm }}
                - name: api-files
                  mountPath: {{ $val.path }}
                  subPath: {{ $key | replace "." "-" }}
                  readOnly: true
                {{- end }}
                {{- end }}
                {{- if and $comp.files $comp.files.secret }}
                {{- range $key, $val := $comp.files.secret }}
                - name: api-files-secret
                  mountPath: {{ $val.path }}
                  subPath: {{ $key | replace "." "-" }}
                  readOnly: true
                {{- end }}
                {{- end }}
                {{- if and $comp.vso $comp.vso.enabled $comp.vso.secretFiles }}
                {{- range $key, $val := $comp.vso.secretFiles }}
                - name: api-vso-files-secret
                  mountPath: {{ $val.path }}
                  subPath: {{ $key | replace "." "-" }}
                  readOnly: true
                {{- end }}
                {{- end }}
          volumes:
                        {{- if and $comp.truststore $comp.truststore.enabled }}
            - name: truststore
              secret:
                secretName: {{ include "chart-model-multi.fullname" . }}-api-secret-truststore
            {{- end }}
            {{- if and $comp.persistence $comp.persistence.enabled }}
            - name: api-data
              {{- if $comp.persistence.ephemeral }}
              emptyDir: {}
              {{- else }}
              persistentVolumeClaim:
                claimName: {{ include "chart-model-multi.fullname" . }}-api
              {{- end }}
            {{- end }}
            {{- if and $comp.files $comp.files.cm (not (empty $comp.files.cm)) }}
            - name: api-files
              configMap:
                name: {{ include "chart-model-multi.fullname" . }}-api-files
            {{- end }}
            {{- if and $comp.files $comp.files.secret (not (empty $comp.files.secret)) }}
            - name: api-files-secret
              secret:
                secretName: {{ include "chart-model-multi.fullname" . }}-api-files-secret
            {{- end }}
            {{- if and $comp.vso $comp.vso.enabled $comp.vso.secretFiles }}
            - name: api-vso-files-secret
              secret:
                secretName: {{ include "chart-model-multi.fullname" . }}-api-vso-secret
            {{- end }}
          restartPolicy: {{ default "OnFailure" $comp.restartPolicy }}
          {{- with $comp.nodeSelector }}
          nodeSelector:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with $comp.affinity }}
          affinity:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with $comp.tolerations }}
          tolerations:
            {{- toYaml . | nindent 12 }}
          {{- end }}
{{- end }}
