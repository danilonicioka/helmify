{{ if and .Values.postgres .Values.postgres.config .Values.postgres.config.env (not (empty .Values.postgres.config.env)) }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "<CHART_NAME>.fullname" . }}-postgres-cm
  labels:
    {{- include "<CHART_NAME>.component.labels" (dict "context" . "component" "postgres") | nindent 4 }}
data:
  {{- range $key, $val := .Values.postgres.config.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
{{- end }}

{{ if .Values.postgres.enabled }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "<CHART_NAME>.fullname" . }}-postgres
  labels:
    {{- include "<CHART_NAME>.component.labels" (dict "context" . "component" "postgres") | nindent 4 }}
  {{- $annotations := include "<CHART_NAME>.component.annotations" (dict "context" . "component" "postgres") }}
  {{- if $annotations }}
  annotations:
    {{- $annotations | nindent 4 }}
  {{- end }}
spec:
  {{- if hasKey .Values.postgres "replicas" }}
  replicas: {{ .Values.postgres.replicas }}
  {{- end }}
  {{- if .Values.postgres.strategy }}
  strategy:
    {{- toYaml .Values.postgres.strategy | nindent 4 }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "<CHART_NAME>.component.selectorLabels" (dict "context" . "component" "postgres") | nindent 6 }}
  template:
    metadata:
      annotations:
        {{- if $annotations }}
        {{- $annotations | nindent 8 }}
        {{- end }}
      labels:
        {{- include "<CHART_NAME>.component.selectorLabels" (dict "context" . "component" "postgres") | nindent 8 }}
    spec:
      {{- with .Values.postgres.image.pullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: postgres
          image: "{{ .Values.postgres.image.repository }}:{{ .Values.postgres.image.tag }}"
          imagePullPolicy: {{ .Values.postgres.image.pullPolicy | default "IfNotPresent" }}
          ports:
            {{- range $key, $val := .Values.postgres.service.ports }}
            - name: {{ $key }}
              containerPort: {{ $val.port }}
              {{- if $val.protocol }}
              protocol: {{ $val.protocol }}
              {{- end }}
            {{- end }}
          
          {{- if or (and .Values.postgres.config .Values.postgres.config.env (not (empty .Values.postgres.config.env))) (and .Values.postgres.secrets .Values.postgres.secrets.env (not (empty .Values.postgres.secrets.env))) (and .Values.global (not (empty .Values.global))) }}
          envFrom:
            {{- if and .Values.postgres.config .Values.postgres.config.env (not (empty .Values.postgres.config.env)) }}
            - configMapRef:
                name: {{ include "<CHART_NAME>.fullname" . }}-postgres-cm
            {{- end }}
            {{- if and .Values.postgres.secrets .Values.postgres.secrets.env (not (empty .Values.postgres.secrets.env)) }}
            - secretRef:
                name: {{ include "<CHART_NAME>.fullname" . }}-postgres-secret
            {{- end }}
            {{- if and .Values.global .Values.global.config .Values.global.config.env (not (empty .Values.global.config.env)) }}
            - configMapRef:
                name: {{ include "<CHART_NAME>.fullname" . }}-global-cm
            {{- end }}
            {{- if and .Values.global .Values.global.secrets .Values.global.secrets.env (not (empty .Values.global.secrets.env)) }}
            - secretRef:
                name: {{ include "<CHART_NAME>.fullname" . }}-global-secret
            {{- end }}
          {{- end }}

          {{- if and .Values.postgres.probes .Values.postgres.probes.startup (not (empty .Values.postgres.probes.startup)) }}
          startupProbe:
            {{- toYaml .Values.postgres.probes.startup | nindent 12 }}
          {{- end }}
          {{- if and .Values.postgres.probes .Values.postgres.probes.liveness (not (empty .Values.postgres.probes.liveness)) }}
          livenessProbe:
            {{- toYaml .Values.postgres.probes.liveness | nindent 12 }}
          {{- end }}
          {{- if and .Values.postgres.probes .Values.postgres.probes.readiness (not (empty .Values.postgres.probes.readiness)) }}
          readinessProbe:
            {{- toYaml .Values.postgres.probes.readiness | nindent 12 }}
          {{- end }}

          {{- with .Values.postgres.command }}
          command:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.postgres.args }}
          args:
            {{- toYaml . | nindent 12 }}
          {{- end }}

          {{- if and .Values.postgres.resources .Values.postgres.resources.limits }}
          resources:
            {{- toYaml .Values.postgres.resources | nindent 12 }}
          {{- end }}

          {{- if or (and .Values.postgres.persistence .Values.postgres.persistence.enabled) (and .Values.postgres.config .Values.postgres.config.files) (and .Values.postgres.secrets .Values.postgres.secrets.files) (and .Values.postgres.vso .Values.postgres.vso.enabled .Values.postgres.vso.files) }}
          volumeMounts:
          {{- if and .Values.postgres.persistence .Values.postgres.persistence.enabled }}
            - name: data
              mountPath: {{ .Values.postgres.persistence.mountPath | default "/var/lib/postgresql/data" }}
          {{- end }}
          {{- if and .Values.postgres.config .Values.postgres.config.files }}
          {{- range $filename, $config := .Values.postgres.config.files }}
            - name: config-files
              mountPath: {{ $config.mountPath | default (printf "/etc/config/%s" $filename) }}
              subPath: {{ $filename }}
          {{- end }}
          {{- end }}
          {{- if and .Values.postgres.secrets .Values.postgres.secrets.files }}
          {{- range $filename, $config := .Values.postgres.secrets.files }}
            - name: secret-files
              mountPath: {{ $config.mountPath | default (printf "/etc/secret/%s" $filename) }}
              subPath: {{ $filename }}
          {{- end }}
          {{- end }}
          {{- if and .Values.postgres.vso .Values.postgres.vso.enabled .Values.postgres.vso.files }}
          {{- range $filename, $config := .Values.postgres.vso.files }}
            - name: vso-files
              mountPath: {{ $config.mountPath | default (printf "/etc/vso/%s" $filename) }}
              subPath: {{ $filename }}
          {{- end }}
          {{- end }}
          {{- end }}
      
      {{- if .Values.postgres.terminationGracePeriodSeconds }}
      terminationGracePeriodSeconds: {{ .Values.postgres.terminationGracePeriodSeconds }}
      {{- end }}

      {{- with .Values.postgres.scheduling.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.postgres.scheduling.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.postgres.scheduling.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .Values.postgres.topologySpreadConstraints }}
      topologySpreadConstraints:
        {{- toYaml .Values.postgres.topologySpreadConstraints | nindent 8 }}
      {{- end }}

      {{- if and .Values.postgres.persistence .Values.postgres.persistence.enabled }}
      volumes:
        - name: data
          {{- if .Values.postgres.persistence.ephemeral }}
          emptyDir: {}
          {{- else }}
          persistentVolumeClaim:
            claimName: {{ include "<CHART_NAME>.fullname" $ }}-postgres
          {{- end }}
      {{- if and .Values.postgres.config .Values.postgres.config.files }}
        - name: config-files
          configMap:
            name: {{ include "<CHART_NAME>.fullname" . }}-postgres-files-cm
      {{- end }}
      {{- if and .Values.postgres.secrets .Values.postgres.secrets.files }}
        - name: secret-files
          secret:
            secretName: {{ include "<CHART_NAME>.fullname" . }}-postgres-files-secret
      {{- end }}
      {{- if and .Values.postgres.vso .Values.postgres.vso.enabled }}
        - name: vso-files
          secret:
            secretName: {{ include "<CHART_NAME>.fullname" . }}-postgres-vso-secret
      {{- end }}
      {{- end }}
{{- end -}}

{{ if and .Values.postgres.enabled (and .Values.postgres.persistence.enabled (not .Values.postgres.persistence.ephemeral)) }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "<CHART_NAME>.fullname" . }}-postgres
  labels:
    {{- include "<CHART_NAME>.component.labels" (dict "context" . "component" "postgres") | nindent 4 }}
spec:
  accessModes:
    - {{ .Values.postgres.persistence.accessMode | default "ReadWriteOnce" }}
  resources:
    requests:
      storage: {{ .Values.postgres.persistence.storageRequest | quote }}
  {{- if .Values.postgres.persistence.storageClass }}
  {{- if (eq "-" .Values.postgres.persistence.storageClass) }}
  storageClassName: ""
  {{- else }}
  storageClassName: "{{ .Values.postgres.persistence.storageClass }}"
  {{- end }}
  {{- end }}
{{- end -}}

{{ if and .Values.postgres .Values.postgres.secrets .Values.postgres.secrets.env (not (empty .Values.postgres.secrets.env)) }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "<CHART_NAME>.fullname" . }}-postgres-secret
  labels:
    {{- include "<CHART_NAME>.component.labels" (dict "context" . "component" "postgres") | nindent 4 }}
type: Opaque
data:
  {{- range $key, $val := .Values.postgres.secrets.env }}
  {{ $key }}: {{ $val | b64enc | quote }}
  {{- end }}
{{- end }}

{{ if .Values.postgres.enabled }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "<CHART_NAME>.fullname" . }}-postgres
  labels:
    {{- include "<CHART_NAME>.component.labels" (dict "context" . "component" "postgres") | nindent 4 }}
spec:
  type: {{ .Values.postgres.service.type | default "ClusterIP" }}
  ports:
    {{- range $key, $val := .Values.postgres.service.ports }}
  - name: {{ $key }}
    port: {{ $val.port }}
    targetPort: {{ $key }}
    {{- if $val.protocol }}
    protocol: {{ $val.protocol }}
    {{- end }}
  {{- end }}
  selector:
    {{- include "<CHART_NAME>.component.selectorLabels" (dict "context" . "component" "postgres") | nindent 4 }}
{{- end -}}

{{- if and .Values.postgres .Values.postgres.config .Values.postgres.config.files }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "<CHART_NAME>.fullname" . }}-postgres-files-cm
  labels:
    {{- include "<CHART_NAME>.labels" . | nindent 4 }}
data:
{{- range $filename, $config := .Values.postgres.config.files }}
  {{ $filename }}: |
    {{- $config.content | nindent 4 }}
{{- end }}
{{- end }}

{{- if and .Values.postgres .Values.postgres.secrets .Values.postgres.secrets.files }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "<CHART_NAME>.fullname" . }}-postgres-files-secret
  labels:
    {{- include "<CHART_NAME>.labels" . | nindent 4 }}
type: Opaque
data:
{{- range $filename, $config := .Values.postgres.secrets.files }}
  {{ $filename }}: {{ if $config.b64enc }}{{ $config.content | quote }}{{ else }}{{ $config.content | b64enc | quote }}{{ end }}
{{- end }}
{{- end }}

{{- if and .Values.postgres .Values.postgres.vso .Values.postgres.vso.enabled }}
---
apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultStaticSecret
metadata:
  name: {{ include "<CHART_NAME>.fullname" . }}-postgres-vso
  labels:
    {{- include "<CHART_NAME>.labels" . | nindent 4 }}
spec:
  type: kv-v2
  mount: kv
  path: {{ .Values.postgres.vso.path }}
  destination:
    name: {{ include "<CHART_NAME>.fullname" . }}-postgres-vso-secret
    create: true
  refreshAfter: 60s
  vaultAuthRef: {{ .Values.postgres.vso.role | default (printf "%s-%s-role" (include "<CHART_NAME>.fullname" .) "postgres") }}
{{- end }}
