{{- range $name, $sidecar := .Values.component.extraContainers }}
{{- if and $sidecar $sidecar.secret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "<CHART_NAME>.fullname" $ }}-{{ $name | replace "." "-" | lower | trimAll "-" }}-secret
  labels:
    {{- include "<CHART_NAME>.labels" $ | nindent 4 }}
type: Opaque
stringData:
  {{- toYaml $sidecar.secret | nindent 2 }}
---
{{- end }}
{{- end }}
{{- range $name, $sidecar := .Values.component.extraContainers }}
{{- if and $sidecar $sidecar.files $sidecar.files.secret }}
{{- range $filename, $config := $sidecar.files.secret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "<CHART_NAME>.fullname" $ }}-{{ $name | replace "." "-" | lower | trimAll "-" }}-{{ $filename | replace "." "-" | lower | trimAll "-" }}-secret
  labels:
    {{- include "<CHART_NAME>.labels" $ | nindent 4 }}
type: Opaque
stringData:
  {{ $filename }}: |-
    {{- $config.content | nindent 4 }}
---
{{- end }}
{{- end }}
{{- end }}
