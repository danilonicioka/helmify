# Standardized TJPA Helm Chart values.yaml (Multi-Deployment Model)
# Designed in compliance with OpenShift Security Context Constraints (SCC)
# and the 6 Core Architectural Rules.

# ==============================================================================
# I. CHART-WIDE OPTIONS
# ==============================================================================
# -- Domain of the Kubernetes cluster.
kubernetesClusterDomain: cluster.local
# -- Overrides the default name/fullname.
nameOverride: ""
fullnameOverride: test-app
# -- Global configuration shared across all deployments in the chart.
global:
  cm:
    TZ: America/Belem
  secret: {}
# ==============================================================================
# II. BACKEND COMPONENT CONFIGURATION
# ==============================================================================
# Note: The component root keys (like 'backend', 'frontend', 'apiService') must be camelCased.
api:
  # ----------------------------------------------------------------------------
  # 1. CORE WORKLOAD SETTINGS (Most Frequently Modified)
  # ----------------------------------------------------------------------------
  # -- Number of replica pods.
  replicas: 1
  # -- Custom labels to add to the backend deployment/pods
  labels:
    k8s-app: fluentd-logging
  # -- Custom annotations to add to the backend deployment 
  annotations: {}
  #   app.openshift.io/connects-to: '[{"apiVersion":"apps/v1","kind":"Deployment","name":"db"}]'

  image:
    # -- Quay Registry is used to avoid Docker Hub rate limiting.
    repository: quay.io/fluentd_elasticsearch/fluentd
    # -- Defaults to Chart.appVersion if not specified.
    tag: v2.5.2
    pullPolicy: IfNotPresent
  # ----------------------------------------------------------------------------
  # 2. APPLICATION CONFIGURATION (Variables & Credentials)
  # ----------------------------------------------------------------------------
  # ConfigMaps unique to this service container.
  cm: {}
  # Sensitive variables unique to this service container (Secrets).
  secret: {}
  # ----------------------------------------------------------------------------
  # 3. ROUTING & NETWORKING
  # ----------------------------------------------------------------------------
  route:
    # -- Shared annotations for routes.
    annotations: {}
    # haproxy.router.openshift.io/timeout: 60s
    # haproxy.router.openshift.io/balance: roundrobin
    # haproxy.router.openshift.io/disable_cookies: "true"

    # -- Shared TLS edge termination configuration.
    tls:
      termination: edge
      insecureEdgeTerminationPolicy: Redirect
    # -- Shared path for routes.
    path: "/api"
    # -- Internal Route with self-signed certificate (to default OCP apps).
    default:
      enabled: true
      host: test-app-backend.apps.ocp-dev.i.tj.pa.gov.br
    # -- Internal Route with valid certificate (to intranet TJPA).
    internal:
      enabled: false
      host: test-app-backend-i.tjpa.jus.br
    # -- External Route with valid certificate (to internet TJPA).
    external:
      enabled: false
      host: test-app-backend.tjpa.jus.br
  # -- Internal Kubernetes Service Configuration.
  service:
    type: ClusterIP
    ports: {}
  # ----------------------------------------------------------------------------
  # 4. RESOURCES (Operational Overlay-Only)
  # ----------------------------------------------------------------------------
  resources:
    limits:
      cpu: 500m
      memory: 200Mi
    requests:
      cpu: 100m
      memory: 200Mi
  # ----------------------------------------------------------------------------
  # 5. TIERED "FAIL-FAST" HEALTH PROBES (tcpSocket & Zero-Delay)
  # ----------------------------------------------------------------------------
  startupProbe:
    tcpSocket:
      port: 8080
    initialDelaySeconds: 0
    periodSeconds: 5
    failureThreshold: 30
    successThreshold: 1

  livenessProbe:
    tcpSocket:
      port: 8080
    initialDelaySeconds: 0
    periodSeconds: 10
    failureThreshold: 3
    successThreshold: 1

  readinessProbe:
    tcpSocket:
      port: 8080
    initialDelaySeconds: 0
    periodSeconds: 10
    failureThreshold: 3
    successThreshold: 1
  # ----------------------------------------------------------------------------
  # 6. LIFECYCLE & HA STRATEGY
  # ----------------------------------------------------------------------------
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 0

  terminationGracePeriodSeconds: 30
  # ----------------------------------------------------------------------------
  # 7. PERSISTENCE
  # ----------------------------------------------------------------------------
  # persistence:
  #   enabled: false
  #   storageClass: thin
  #   storageRequest: 512Mi

  imagePullPolicy: IfNotPresent
  nodeSelector: {}
  command:
    - perl
    - -Mbignum=bpi
    - -wle
    - print bpi(2000)
    - /bin/sh
    - -c
    - date; echo Hello from the Kubernetes cluster
  schedule: '* * * * *'
  tolerations:
    - effect: NoSchedule
      key: node-role.kubernetes.io/master
      operator: Exists
  topologySpreadConstraints: []
  backoffLimit: 4
# ==============================================================================
# III. FRONTEND COMPONENT CONFIGURATION
# ==============================================================================
app-emissor:
  # ----------------------------------------------------------------------------
  # 1. CORE WORKLOAD SETTINGS
  # ----------------------------------------------------------------------------
  replicas: 2
  # -- Custom labels to add to the frontend deployment/pods
  labels: {}
  #   app.openshift.io/runtime: openjdk

  # -- Custom annotations to add to the frontend deployment 
  annotations: {}
  #   app.openshift.io/connects-to: '[{"apiVersion":"apps/v1","kind":"Deployment","name":"db"}]'

  image:
    repository: registry.k8s.io/nginx-slim
    tag: "0.8"
    pullPolicy: IfNotPresent
  # ----------------------------------------------------------------------------
  # 2. APPLICATION CONFIGURATION
  # ----------------------------------------------------------------------------
  cm: {}
  # No default secrets for web frontend component
  secret: {}
  # ----------------------------------------------------------------------------
  # 3. ROUTING & NETWORKING
  # ----------------------------------------------------------------------------
  route:
    annotations: {}
    #   app.openshift.io/connects-to: '[{"apiVersion":"apps/v1","kind":"Deployment","name":"db"}]'

    # haproxy.router.openshift.io/timeout: 60s
    # haproxy.router.openshift.io/balance: roundrobin
    # haproxy.router.openshift.io/disable_cookies: "true"
    tls:
      termination: edge
      insecureEdgeTerminationPolicy: Redirect
    path: "/"
    default:
      enabled: true
      host: test-app-frontend.apps.ocp-dev.i.tj.pa.gov.br
    internal:
      enabled: false
      host: test-app-frontend-i.tjpa.jus.br
    external:
      enabled: false
      host: test-app-frontend.tjpa.jus.br
  service:
    type: ClusterIP
    ports: {}
  # ----------------------------------------------------------------------------
  # 4. RESOURCES
  # ----------------------------------------------------------------------------
  resources:
    limits:
      cpu: 500m
      memory: 256Mi
    requests:
      cpu: 100m
      memory: 128Mi
  # ----------------------------------------------------------------------------
  # 5. TIERED "FAIL-FAST" HEALTH PROBES (tcpSocket & Zero-Delay)
  # ----------------------------------------------------------------------------
  startupProbe:
    tcpSocket:
      port: 8080
    initialDelaySeconds: 0
    periodSeconds: 5
    failureThreshold: 30
    successThreshold: 1

  livenessProbe:
    tcpSocket:
      port: 8080
    initialDelaySeconds: 0
    periodSeconds: 10
    failureThreshold: 3
    successThreshold: 1

  readinessProbe:
    tcpSocket:
      port: 8080
    initialDelaySeconds: 0
    periodSeconds: 10
    failureThreshold: 3
    successThreshold: 1
  # ----------------------------------------------------------------------------
  # 6. LIFECYCLE & HA STRATEGY
  # ----------------------------------------------------------------------------
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 0

  terminationGracePeriodSeconds: 30
  volumeClaims:
    www:
      requests:
        storage: 1Gi
  tolerations: []
  topologySpreadConstraints: []
  nodeSelector: {}
# ==============================================================================
# III. FRONTEND COMPONENT CONFIGURATION
# ==============================================================================
myapp:
  # ----------------------------------------------------------------------------
  # 1. CORE WORKLOAD SETTINGS
  # ----------------------------------------------------------------------------
  replicas: 3
  # -- Custom labels to add to the frontend deployment/pods
  labels: {}
  #   app.openshift.io/runtime: openjdk

  # -- Custom annotations to add to the frontend deployment 
  annotations: {}
  #   app.openshift.io/connects-to: '[{"apiVersion":"apps/v1","kind":"Deployment","name":"db"}]'

  image:
    repository: tjpa-registry-quay-quay-enterprise.apps.ocp-hub.i.tj.pa.gov.br/devops/test-app-frontend
    tag: "latest"
    pullPolicy: IfNotPresent
  # ----------------------------------------------------------------------------
  # 2. APPLICATION CONFIGURATION
  # ----------------------------------------------------------------------------
  cm:
    dummyconfigmapkey: dummyconfigmapvalue
    my_config.properties: |
      health.healthProbeBindAddress=8081
      metrics.bindAddress=127.0.0.1:8080
    my.prop1: "1"
    my.prop2: val 1
    my.prop3: "true"
    myval.yaml: |
      apiVersion: clickhouse.altinity.com/v1
      kind: ClickHouseInstallationTemplate
      metadata:
        name: default-oneperhost-pod-template
      spec:
        templates:
          podTemplates:
            - name: default-oneperhost-pod-template
              distribution: "OnePerHost"
  # No default secrets for web frontend component
  secret:
    ELASTIC_FOOBAR_HUNTER123_MEOWTOWN_VERIFY: my_secret_var_2
    str: |
      some big not so secret string with
      multiple lines
    VAR1: my_secret_var_1
    ca.crt: |-
      superlongtestcrt-superlongtestcrt-superlongtestcrt-superlongtestcrt-superlongtestcrt-superlongtestcrt-superlongtestcrt-
      superlongtestcrt-superlongtestcrt-superlongtestcrt-superlongtestcrt-superlongtestcrt-superlongtestcrt
      superlongtestcrt-superlongtestcrt-superlongtestcrt-superlongtestcrt-superlongtestcrt-superlongtestcrt
    VAR2: my_secret_var_2
  # ----------------------------------------------------------------------------
  # 3. ROUTING & NETWORKING
  # ----------------------------------------------------------------------------
  route:
    annotations: {}
    #   app.openshift.io/connects-to: '[{"apiVersion":"apps/v1","kind":"Deployment","name":"db"}]'

    # haproxy.router.openshift.io/timeout: 60s
    # haproxy.router.openshift.io/balance: roundrobin
    # haproxy.router.openshift.io/disable_cookies: "true"
    tls:
      termination: edge
      insecureEdgeTerminationPolicy: Redirect
    path: "/"
    default:
      enabled: true
      host: test-app-frontend.apps.ocp-dev.i.tj.pa.gov.br
    internal:
      enabled: false
      host: test-app-frontend-i.tjpa.jus.br
    external:
      enabled: false
      host: test-app-frontend.tjpa.jus.br
  service:
    type: ClusterIP
    ports:
      https:
        port: 8443
        targetPort: https
    loadBalancerSourceRanges:
      - 10.0.0.0/8
    ipFamilyPolicy: SingleStack
    ipFamilies:
      - IPv4
  # ----------------------------------------------------------------------------
  # 4. RESOURCES
  # ----------------------------------------------------------------------------
  resources:
    limits:
      cpu: 500m
      memory: 256Mi
    requests:
      cpu: 100m
      memory: 128Mi
  # ----------------------------------------------------------------------------
  # 5. TIERED "FAIL-FAST" HEALTH PROBES (tcpSocket & Zero-Delay)
  # ----------------------------------------------------------------------------
  startupProbe:
    tcpSocket:
      port: 8080
    initialDelaySeconds: 0
    periodSeconds: 5
    failureThreshold: 30
    successThreshold: 1

  livenessProbe:
    tcpSocket:
      port: 8080
    initialDelaySeconds: 0
    periodSeconds: 10
    failureThreshold: 3
    successThreshold: 1

  readinessProbe:
    tcpSocket:
      port: 8080
    initialDelaySeconds: 0
    periodSeconds: 10
    failureThreshold: 3
    successThreshold: 1
  # ----------------------------------------------------------------------------
  # 6. LIFECYCLE & HA STRATEGY
  # ----------------------------------------------------------------------------
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 0

  terminationGracePeriodSeconds: 10
  tolerations: []
  podSecurityContext:
    runAsNonRoot: true
    fsGroup: 20000
    runAsUser: 65532
  app:

    readinessProbe:
      initialDelaySeconds: 5
      periodSeconds: 10
      httpGet:
        port: 8081
        path: /readyz
    args:
      - --health-probe-bind-address=:8081
      - --metrics-bind-address=127.0.0.1:8080
      - --leader-elect
    command:
      - /manager
    containerSecurityContext: {}
    image:
      repository: controller
      tag: latest
    resources:
      requests:
        memory: 20Mi
        cpu: 100m
      limits:
        memory: 30Mi
        cpu: 100m

    livenessProbe:
      httpGet:
        path: /healthz
        port: 8081
      initialDelaySeconds: 15
      periodSeconds: 20
  topologySpreadConstraints: []
  command:
    - /bin/sh
    - -c
    - echo 'Initializing container...'
  proxySidecar:
    image:
      tag: v0.8.0
      repository: gcr.io/kubebuilder/kube-rbac-proxy
    args:
      - --secure-listen-address=0.0.0.0:8443
      - --v=10
  nodeSelector:
    region: east
    type: user-node

  persistence:
    storageLimit: 5Gi
    enabled: true
    storageClass: manual
    storageRequest: 3Gi
  initContainer:
    image:
      repository: bash
      tag: latest
  revisionHistoryLimit: 5
# ==============================================================================
# II. BACKEND COMPONENT CONFIGURATION
# ==============================================================================
# Note: The component root keys (like 'backend', 'frontend', 'apiService') must be camelCased.
nginx:
  # ----------------------------------------------------------------------------
  # 1. CORE WORKLOAD SETTINGS (Most Frequently Modified)
  # ----------------------------------------------------------------------------
  # -- Number of replica pods.
  replicas: 1
  # -- Custom labels to add to the backend deployment/pods
  labels: {}
  #   app.openshift.io/runtime: openjdk

  # -- Custom annotations to add to the backend deployment 
  annotations: {}
  #   app.openshift.io/connects-to: '[{"apiVersion":"apps/v1","kind":"Deployment","name":"db"}]'

  image:
    # -- Quay Registry is used to avoid Docker Hub rate limiting.
    repository: tjpa-registry-quay-quay-enterprise.apps.ocp-hub.i.tj.pa.gov.br/devops/test-app-backend
    # -- Defaults to Chart.appVersion if not specified.
    tag: "latest"
    pullPolicy: IfNotPresent
  # ----------------------------------------------------------------------------
  # 2. APPLICATION CONFIGURATION (Variables & Credentials)
  # ----------------------------------------------------------------------------
  # ConfigMaps unique to this service container.
  cm: {}
  # Sensitive variables unique to this service container (Secrets).
  secret: {}
  # ----------------------------------------------------------------------------
  # 3. ROUTING & NETWORKING
  # ----------------------------------------------------------------------------
  route:
    # -- Shared annotations for routes.
    annotations: {}
    # haproxy.router.openshift.io/timeout: 60s
    # haproxy.router.openshift.io/balance: roundrobin
    # haproxy.router.openshift.io/disable_cookies: "true"

    # -- Shared TLS edge termination configuration.
    tls:
      termination: edge
      insecureEdgeTerminationPolicy: Redirect
    # -- Shared path for routes.
    path: "/api"
    # -- Internal Route with self-signed certificate (to default OCP apps).
    default:
      enabled: true
      host: test-app-backend.apps.ocp-dev.i.tj.pa.gov.br
    # -- Internal Route with valid certificate (to intranet TJPA).
    internal:
      enabled: false
      host: test-app-backend-i.tjpa.jus.br
    # -- External Route with valid certificate (to internet TJPA).
    external:
      enabled: false
      host: test-app-backend.tjpa.jus.br
  # -- Internal Kubernetes Service Configuration.
  service:
    type: ClusterIP
    ports:
      web:
        port: 80
  # ----------------------------------------------------------------------------
  # 4. RESOURCES (Operational Overlay-Only)
  # ----------------------------------------------------------------------------
  resources:
    limits:
      cpu: 500m
      memory: 256Mi
    requests:
      cpu: 100m
      memory: 128Mi
  # ----------------------------------------------------------------------------
  # 5. TIERED "FAIL-FAST" HEALTH PROBES (tcpSocket & Zero-Delay)
  # ----------------------------------------------------------------------------
  startupProbe:
    tcpSocket:
      port: 8080
    initialDelaySeconds: 0
    periodSeconds: 5
    failureThreshold: 30
    successThreshold: 1

  livenessProbe:
    tcpSocket:
      port: 8080
    initialDelaySeconds: 0
    periodSeconds: 10
    failureThreshold: 3
    successThreshold: 1

  readinessProbe:
    tcpSocket:
      port: 8080
    initialDelaySeconds: 0
    periodSeconds: 10
    failureThreshold: 3
    successThreshold: 1
  # ----------------------------------------------------------------------------
  # 6. LIFECYCLE & HA STRATEGY
  # ----------------------------------------------------------------------------
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 0

  terminationGracePeriodSeconds: 30
  # ----------------------------------------------------------------------------
  # 7. PERSISTENCE
  # ----------------------------------------------------------------------------
  # persistence:
  #   enabled: false
  #   storageClass: thin
  #   storageRequest: 512Mi

  minAvailable: 2
# ==============================================================================
# IV. GLOBAL SCHEDULING & COMMON CONFIGS
# ==============================================================================
imagePullSecrets: []
nodeSelector: {}
tolerations: []
affinity:
  podAntiAffinity:
    preferredDuringSchedulingIgnoredDuringExecution:
      - weight: 100
        podAffinityTerm:
          labelSelector:
            matchExpressions:
              - key: app.kubernetes.io/name
                operator: In
                values:
                  - test-app
          topologyKey: kubernetes.io/hostname
