# Standardized Standardized Helm Chart values.yaml
# Designed in compliance with OpenShift Security Context Constraints (SCC)
# and the 6 Core Architectural Rules.

# ==============================================================================
# I. CHART-WIDE OPTIONS
# ==============================================================================
# -- Domain of the Kubernetes cluster.
kubernetesClusterDomain: cluster.local
# -- Overrides the default name/fullname.
nameOverride: ""
fullnameOverride: test-app
# -- Global configuration shared across all deployments in the chart.
global:
  cm: {}

  secret: {}
# ==============================================================================
# II. API COMPONENT CONFIGURATION
# ==============================================================================
api:
  # ----------------------------------------------------------------------------
  # 1. IMAGE & WORKLOAD
  # ----------------------------------------------------------------------------
  image:
    repository: quay.io/fluentd_elasticsearch/fluentd
    tag: v2.5.2
    pullPolicy: IfNotPresent
    pullSecrets: []

  replicas: 1

  labels:
    k8s-app: fluentd-logging
  annotations: {}
  # Example for OpenShift Topology View integration:
  # app.openshift.io/connects-to: '[{"apiVersion":"apps/v1","kind":"Deployment","name":"test-app-app"}]'

  # ----------------------------------------------------------------------------
  # 2. APPLICATION CONFIGURATION & SECRETS
  # ----------------------------------------------------------------------------
  cm: {}

  secret: {}

  vso:
    enabled: false
    role: "test-app-api-role"
    path: "kv/data/test-app/api"
    files: {}
    #   my_vault_key.p12:
    #     mountPath: "/app/certs/my_vault_key.p12"
  truststore:
    enabled: false
    path: "/custom-truststore"
    javaToolOptions: "-Djavax.net.ssl.trustStore=/custom-truststore/cacerts -Djavax.net.ssl.trustStorePassword=changeit"
    certificate: ""
  files:
    cm: {}
    #   application.properties:
    #     mountPath: "/deployments/config/application.properties"
    #     content: |
    #       quarkus.http.port=8080
    secret: {}
    #   sistema.p12:
    #     mountPath: "/app/keystore/sistema.p12"
    #     b64enc: true
    #     content: "MIIKrAIB..."
  # ----------------------------------------------------------------------------
  # 3. RESOURCES
  # ----------------------------------------------------------------------------
  # Baseline resource allocation overlay. Adjust to application requirements.
  resources:
    limits:
      cpu: 100m
      memory: 200Mi
    requests:
      cpu: 100m
      memory: 200Mi
  hpa:
    enabled: false
    minReplicas: 1
    maxReplicas: 2
    metrics:
      - type: Resource
        resource:
          name: memory
          target:
            type: Utilization
            averageUtilization: 180
    behavior:
      scaleDown:
        stabilizationWindowSeconds: 120
      scaleUp:
        stabilizationWindowSeconds: 0
        policies:
          - type: Percent
            value: 100
            periodSeconds: 15
  # ----------------------------------------------------------------------------
  # 4. ROUTING & NETWORKING
  # ----------------------------------------------------------------------------
  route:
    annotations: {}
    #   app.openshift.io/connects-to: '[{"apiVersion":"apps/v1","kind":"Deployment","name":"db"}]'

    tls:
      termination: edge
      insecureEdgeTerminationPolicy: Redirect
    path: "/"
    default:
      enabled: true
      host: test-app-api{{DEFAULT_DOMAIN}}
      annotations: {}
      #   app.openshift.io/connects-to: '[{"apiVersion":"apps/v1","kind":"Deployment","name":"db"}]'
    internal:
      enabled: false
      host: test-app-api{{INTERNAL_DOMAIN}}
      annotations: {}
      #   app.openshift.io/connects-to: '[{"apiVersion":"apps/v1","kind":"Deployment","name":"db"}]'
    external:
      enabled: false
      host: test-app-api{{EXTERNAL_DOMAIN}}
      annotations: {}
      #   app.openshift.io/connects-to: '[{"apiVersion":"apps/v1","kind":"Deployment","name":"db"}]'
    additional: {}
    #   custom-route:
    #     enabled: true
    #     host: custom-route.domain.com
    #     path: "/custom"
    #     annotations: {}
    #     tls:
    #       termination: edge
    #       insecureEdgeTerminationPolicy: Redirect
  service:
    type: ClusterIP
    ports:
      http:
        # Note: The 'port' defined here applies to BOTH the Kubernetes Service port and the underlying Container port.
        port: 8080
  # ----------------------------------------------------------------------------
  # 5. PERSISTENCE
  # ----------------------------------------------------------------------------
  persistence:
    enabled: false
    ephemeral: false
    mountPath: "/var/lib/data"
    storageRequest: 1Gi
    accessMode: ReadWriteOnce
    storageClass: ""
  # ----------------------------------------------------------------------------
  # 6. PROBES & LIFECYCLE
  # ----------------------------------------------------------------------------
  startupProbe:
    tcpSocket:
      port: http
    initialDelaySeconds: 0
    periodSeconds: 5
    failureThreshold: 30
    successThreshold: 1

  livenessProbe:
    tcpSocket:
      port: http
    initialDelaySeconds: 0
    periodSeconds: 10
    failureThreshold: 3
    successThreshold: 1

  readinessProbe:
    tcpSocket:
      port: http
    initialDelaySeconds: 0
    periodSeconds: 10
    failureThreshold: 3
    successThreshold: 1

  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 0

  terminationGracePeriodSeconds: 30
  # ----------------------------------------------------------------------------
  # 7. SCHEDULING & NODE ASSIGNMENT
  # ----------------------------------------------------------------------------
  nodeSelector: {}

  tolerations:
    - effect: NoSchedule
      key: node-role.kubernetes.io/master
      operator: Exists

  affinity:
    podAntiAffinity:
      preferredDuringSchedulingIgnoredDuringExecution:
        - weight: 100
          podAffinityTerm:
            labelSelector:
              matchExpressions:
                - key: app.kubernetes.io/component
                  operator: In
                  values:
                    - test-app-api
            topologyKey: kubernetes.io/hostname
  backoffLimit: 4
  command:
    - perl
    - -Mbignum=bpi
    - -wle
    - print bpi(2000)
    - /bin/sh
    - -c
    - date; echo Hello from the Kubernetes cluster
  imagePullPolicy: IfNotPresent
  schedule: '* * * * *'
  topologySpreadConstraints: []
# ==============================================================================
# III. APP COMPONENT CONFIGURATION
# ==============================================================================
app:
  # ----------------------------------------------------------------------------
  # 1. IMAGE & WORKLOAD
  # ----------------------------------------------------------------------------
  image:
    repository: registry.k8s.io/nginx-slim
    tag: "0.8"
    pullPolicy: IfNotPresent
    pullSecrets: []

  replicas: 2

  labels: {}
  #   app.openshift.io/runtime: openjdk

  annotations: {}
  # Example for OpenShift Topology View integration:
  # app.openshift.io/connects-to: '[{"apiVersion":"apps/v1","kind":"Deployment","name":"test-app-api"}]'

  # ----------------------------------------------------------------------------
  # 2. APPLICATION CONFIGURATION & SECRETS
  # ----------------------------------------------------------------------------
  cm: {}

  secret: {}

  vso:
    enabled: false
    role: "test-app-app-role"
    path: "kv/data/test-app/app"
    files: {}
  truststore:
    enabled: false
    path: "/custom-truststore"
    javaToolOptions: "-Djavax.net.ssl.trustStore=/custom-truststore/cacerts -Djavax.net.ssl.trustStorePassword=changeit"
    certificate: ""
  files:
    cm: {}
    #   nginx.conf:
    #     mountPath: "/etc/nginx/nginx.conf"
    #     content: |
    #       user nginx;
    #       worker_processes auto;
    secret: {}
    #   sistema.p12:
    #     mountPath: "/app/keystore/sistema.p12"
    #     b64enc: true
    #     content: "MIIKrAIB..."
  # ----------------------------------------------------------------------------
  # 3. RESOURCES
  # ----------------------------------------------------------------------------
  # Baseline resource allocation overlay. Adjust to application requirements.
  resources:
    limits:
      cpu: 100m
      memory: 512Mi
    requests:
      cpu: 30m
      memory: 256Mi
  hpa:
    enabled: false
    minReplicas: 1
    maxReplicas: 2
    metrics:
      - type: Resource
        resource:
          name: memory
          target:
            type: Utilization
            averageUtilization: 180
    behavior:
      scaleDown:
        stabilizationWindowSeconds: 120
      scaleUp:
        stabilizationWindowSeconds: 0
        policies:
          - type: Percent
            value: 100
            periodSeconds: 15
  # ----------------------------------------------------------------------------
  # 4. ROUTING & NETWORKING
  # ----------------------------------------------------------------------------
  route:
    annotations: {}
    #   app.openshift.io/connects-to: '[{"apiVersion":"apps/v1","kind":"Deployment","name":"db"}]'

    tls:
      termination: edge
      insecureEdgeTerminationPolicy: Redirect
    path: "/"
    default:
      enabled: true
      host: test-app-app{{DEFAULT_DOMAIN}}
      annotations: {}
      #   app.openshift.io/connects-to: '[{"apiVersion":"apps/v1","kind":"Deployment","name":"db"}]'
    internal:
      enabled: false
      host: test-app-app{{INTERNAL_DOMAIN}}
      annotations: {}
      #   app.openshift.io/connects-to: '[{"apiVersion":"apps/v1","kind":"Deployment","name":"db"}]'
    external:
      enabled: false
      host: test-app-app{{EXTERNAL_DOMAIN}}
      annotations: {}
      #   app.openshift.io/connects-to: '[{"apiVersion":"apps/v1","kind":"Deployment","name":"db"}]'
    additional: {}
    #   custom-route:
    #     enabled: true
    #     host: custom-route.domain.com
    #     path: "/custom"
    #     annotations: {}
    #     tls:
    #       termination: edge
    #       insecureEdgeTerminationPolicy: Redirect
  service:
    type: ClusterIP
    ports:
      http:
        # Note: The 'port' defined here applies to BOTH the Kubernetes Service port and the underlying Container port.
        port: 8080
  # ----------------------------------------------------------------------------
  # 5. PERSISTENCE
  # ----------------------------------------------------------------------------
  persistence:
    enabled: false
    ephemeral: false
    mountPath: "/var/lib/data"
    storageRequest: 1Gi
    accessMode: ReadWriteOnce
    storageClass: ""
  # ----------------------------------------------------------------------------
  # 6. PROBES & LIFECYCLE
  # ----------------------------------------------------------------------------
  startupProbe:
    tcpSocket:
      port: http
    initialDelaySeconds: 0
    periodSeconds: 5
    failureThreshold: 30
    successThreshold: 1

  livenessProbe:
    tcpSocket:
      port: http
    initialDelaySeconds: 0
    periodSeconds: 10
    failureThreshold: 3
    successThreshold: 1

  readinessProbe:
    tcpSocket:
      port: http
    initialDelaySeconds: 0
    periodSeconds: 10
    failureThreshold: 3
    successThreshold: 1

  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 0

  terminationGracePeriodSeconds: 30
  # ----------------------------------------------------------------------------
  # 7. SCHEDULING & NODE ASSIGNMENT
  # ----------------------------------------------------------------------------
  nodeSelector: {}

  tolerations: []

  affinity:
    podAntiAffinity:
      preferredDuringSchedulingIgnoredDuringExecution:
        - weight: 100
          podAffinityTerm:
            labelSelector:
              matchExpressions:
                - key: app.kubernetes.io/component
                  operator: In
                  values:
                    - test-app-app
            topologyKey: kubernetes.io/hostname
  topologySpreadConstraints: []
  volumeClaims:
    www:
      requests:
        storage: 1Gi
myapp:
  annotations: {}
  #   app.openshift.io/connects-to: '[{"apiVersion":"apps/v1","kind":"Deployment","name":"db"}]'

  app:
    args:
      - --health-probe-bind-address=:8081
      - --metrics-bind-address=127.0.0.1:8080
      - --leader-elect
    command:
      - /manager
    containerSecurityContext: {}
    image:
      repository: controller
      tag: latest

    livenessProbe:
      httpGet:
        path: /healthz
        port: 8081
      initialDelaySeconds: 15
      periodSeconds: 20

    readinessProbe:
      httpGet:
        path: /readyz
        port: 8081
      initialDelaySeconds: 5
      periodSeconds: 10

    resources:
      limits:
        cpu: 100m
        memory: 30Mi
      requests:
        cpu: 100m
        memory: 20Mi

  cm:
    dummyconfigmapkey: dummyconfigmapvalue
    my.prop1: "1"
    my.prop2: val 1
    my.prop3: "true"
    my_config.properties: |
      health.healthProbeBindAddress=8081
      metrics.bindAddress=127.0.0.1:8080
    myval.yaml: |
      apiVersion: clickhouse.altinity.com/v1
      kind: ClickHouseInstallationTemplate
      metadata:
        name: default-oneperhost-pod-template
      spec:
        templates:
          podTemplates:
            - name: default-oneperhost-pod-template
              distribution: "OnePerHost"
  command:
    - /bin/sh
    - -c
    - echo 'Initializing container...'
  initContainer:
    image:
      repository: bash
      tag: latest

  labels: {}
  #   app.openshift.io/runtime: openjdk

  nodeSelector:
    region: east
    type: user-node

  persistence:
    enabled: true
    storageClass: manual
    storageLimit: 5Gi
    storageRequest: 3Gi
  podSecurityContext:
    fsGroup: 20000
    runAsNonRoot: true
    runAsUser: 65532
  proxySidecar:
    args:
      - --secure-listen-address=0.0.0.0:8443
      - --v=10
    image:
      repository: gcr.io/kubebuilder/kube-rbac-proxy
      tag: v0.8.0

  replicas: 3
  revisionHistoryLimit: 5

  secret:
    ELASTIC_FOOBAR_HUNTER123_MEOWTOWN_VERIFY: my_secret_var_2
    VAR1: my_secret_var_1
    VAR2: my_secret_var_2
    ca.crt: |-
      superlongtestcrt-superlongtestcrt-superlongtestcrt-superlongtestcrt-superlongtestcrt-superlongtestcrt-superlongtestcrt-
      superlongtestcrt-superlongtestcrt-superlongtestcrt-superlongtestcrt-superlongtestcrt-superlongtestcrt
      superlongtestcrt-superlongtestcrt-superlongtestcrt-superlongtestcrt-superlongtestcrt-superlongtestcrt
    str: |
      some big not so secret string with
      multiple lines

  service:
    ipFamilies:
      - IPv4
    ipFamilyPolicy: SingleStack
    loadBalancerSourceRanges:
      - 10.0.0.0/8
    ports:
      https:
        port: 8443
        targetPort: https
    type: ClusterIP

  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 0
    type: RollingUpdate

  terminationGracePeriodSeconds: 10

  tolerations: []
  topologySpreadConstraints: []
nginx:
  minAvailable: 2

  service:
    ports:
      web:
        port: 80
    type: ClusterIP
